import 'package:flutter/material.dart';

class LFReport extends StatelessWidget {
  const LFReport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Lost and Found"),
    );
  }
}
